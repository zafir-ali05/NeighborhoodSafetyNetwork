import 'package:flutter/material.dart';

class SilentAlertPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Silent Alert'),
      ),
      body: Center(
        child: Text('Silent Alert Page Content'),
      ),
    );
  }
}